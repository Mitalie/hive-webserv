#include <iostream>

int main()
{
	std::cout << "This is an example test implemented in `" __FILE__ "'.\n"
				 "Add more .cpp files in the same directory, each with their own main function.\n"
				 "Don't forget to add them to TESTS in Makefile (without the .cpp suffix).\n";
}
