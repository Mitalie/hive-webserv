server {
	listen 127.0.0.1 8080;
	server_name localhost;
	client_max_body_size 100;  # 100 bytes limit for testing

	location / {
		root ./testdata;
		methods GET POST;
	}

	location /uploads {
		root ./uploads;
		methods GET POST;
	}
}
